// rollup.config.js
import image from '@rollup/plugin-image';  // To handle image files
import url from '@rollup/plugin-url';      // To handle importing images as URLs

export default {
  input: 'src/index.js',  // Main entry file (change if necessary)
  output: {
    dir: 'dist',           // The output directory
    format: 'es',          // Module format (ES modules in this case)
  },
  plugins: [
    image(),   // Handle images (base64 or file copies)
    url(),     // Handle image imports as URLs
  ],
};
